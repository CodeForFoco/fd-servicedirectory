import styled from "styled-components";

const Box = styled.div({
  background: "#fff",
  borderRadius: "4px",
  boxShadow: "0px 1px 4px rgba(36, 34, 40, 0.08)",
  padding: "24px",
});

export default Box;
